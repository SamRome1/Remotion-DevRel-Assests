import React from 'react';
import { NewProjectScene } from '../scenes/newProject';

export { NEW_PROJECT_DURATION } from '../scenes/newProject';

/** NewProject — 16:9 YouTube explainer, white canvas (Segment 3: every new question is a whole new project). */
export const NewProject: React.FC = () => <NewProjectScene />;
