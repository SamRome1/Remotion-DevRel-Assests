import React from 'react';
import { MlPipelineScene } from '../scenes/mlPipeline';

export { ML_PIPELINE_DURATION } from '../scenes/mlPipeline';

/** MlPipeline — 16:9 YouTube explainer, white canvas (Segment 3: building a classifier the old way). */
export const MlPipeline: React.FC = () => <MlPipelineScene />;
