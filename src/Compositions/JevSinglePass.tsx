import React from 'react';
import { JevSinglePassScene } from '../scenes/jevSinglePass';

export { JEV_SINGLE_PASS_DURATION } from '../scenes/jevSinglePass';

/** JevSinglePass — 16:9 YouTube explainer, white canvas (Segment 2 of the hammer/screwdriver script). */
export const JevSinglePass: React.FC = () => <JevSinglePassScene />;
