import React from 'react';
import { ClassifiersScene } from '../scenes/classifiers';

export { CLASSIFIERS_DURATION } from '../scenes/classifiers';

/** Classifiers — 16:9 YouTube explainer, white canvas (Segment 3 of the hammer/screwdriver script). */
export const Classifiers: React.FC = () => <ClassifiersScene />;
