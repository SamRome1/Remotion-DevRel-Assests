import React from 'react';
import { EverySwingScene } from '../scenes/everySwing';

export { EVERY_SWING_DURATION } from '../scenes/everySwing';

/**
 * EverySwing — 16:9 YouTube explainer, white canvas (companion to OneTool).
 * VO: "Every swing takes seconds."
 */
export const EverySwing: React.FC = () => <EverySwingScene />;
