import React from 'react';
import { HammerPricesScene } from '../scenes/hammerPrices';

export { HAMMER_PRICES_DURATION } from '../scenes/hammerPrices';

/** HammerPrices — 16:9 YouTube explainer, white canvas (Segment 3 closer: hammer prices for screwdriver jobs). */
export const HammerPrices: React.FC = () => <HammerPricesScene />;
